# Промпт: Визуальный интерфейс — Сайт-визитка с QR-кодом и реферальной системой

> Связанные документы: [PRD](../prd/README.md) | [Отчёт по полноте PRD](../prd/PRD_completeness_report.md) | [Архитектура](../architecture/README.md) | [Инфраструктура](../infrastructure/README.md) | [План развития](../roadmap/README.md) | [BDD](../bdd/README.md)

## Контекст

Сайт-визитка для сбора заявок с офлайн-носителей (QR-коды на визитках/рекламе) и партнёрских каналов. Пользователь сканирует QR → попадает на лендинг → выбирает услугу → оставляет заявку. Параллельно партнёры получают реферальные ссылки и привлекают клиентов.

Бренд: **ВСЯК** — универсальный справочник услуг для новоселов.

## Стек

- Svelte (SvelteKit или Vite + Svelte)
- UnoCSS (Tailwind-совместимый синтаксис через `@unocss/preset-wind`)
- Mobile-first (QR сканируют с телефона)

## Дизайн-система

### Логотип

Чистый современный вортмарк **«ВСЯК»** — мягкий скруглённый sans-serif, буквы тёмного графитового цвета, последняя буква **«К»** — мягкий голубой (#A5C4E8). Центрирован в верхней части страницы. Элегантный и минималистичный.

### Палитра цветов (Soft Sage & Cream)

| Роль | Цвет | HEX |
|---|---|---|
| Primary | Soft sage green | `#6B9B7A` |
| Primary hover | Darker sage | `#5A8A69` |
| Background | Warm cream | `#F9F7F2` |
| Surface / cards | Pure white | `#FFFFFF` |
| Text primary | Warm dark gray | `#2D2A26` |
| Text secondary | Muted warm gray | `#7A736A` |
| Borders | Soft warm gray | `#E8E4DC` |
| Success | Soft mint | `#81B29A` |
| Error | Soft coral | `#E07A5F` |

### Типографика

- Заголовки: системный sans-serif (Inter / San Francisco)
- Основной текст: 16px, line-height 1.5
- Заголовки секций: 24–32px, font-weight 600
- Мелкий текст: 12–14px

### Отступы и размеры

- Базовый отступ: 8px (multiples of 4px)
- Border-radius: 12px (карточки), 8px (инпуты), 24px (кнопки)
- Тени: `0 2px 8px rgba(0,0,0,0.06)` (карточки), `0 4px 16px rgba(0,0,0,0.08)` (hover)

## UI-Kit: Промпт для генерации

Создай современный UI-кит и набор страниц для сервиса «ВСЯК» (сайт-визитка с формой заявки и реферальной системой).

Бренд:
- Логотип: чистый современный wordmark «ВСЯК» — мягкий скруглённый sans-serif. Буквы тёмно-угольные, последняя буква «К» светло-голубая (#A5C4E8).
- Стиль: тёплый, мягкий, премиальный, минималистичный, mobile-first.

Цветовая палитра Soft Sage & Cream:
- Primary: мягкий шалфейный #6B9B7A
- Primary hover: #5A8A69
- Фон: тёплый кремовый #F9F7F2
- Карточки / поверхности: чисто белый #FFFFFF
- Основной текст: тёплый тёмно-серый #2D2A26
- Вторичный текст: приглушённый тёплый серый #7A736A
- Границы: мягкий тёплый серый #E8E4DC
- Успех: мягкий мятный #81B29A
- Ошибка: мягкий коралловый #E07A5F

### 1. Общий layout для публичных страниц

Используется на: Лендинг (/), Страница «Спасибо» (/thanks), Политика конфиденциальности (/privacy)

Структура:
- Верхний отступ 24–32px
- По центру логотип «ВСЯК»
- Основной контент в контейнере max-width 480px (на мобиле на всю ширину с отступами 20px)
- Много «воздуха» (кремовый фон)
- Мягкие скругления 12–16px
- Лёгкие тени только у карточек
- Внизу небольшой футер с ссылкой на политику конфиденциальности

Стиль: очень чистый, тёплый, спокойный, много кремового пространства.

### Реализация layout-компонентов

**Файловая структура:**

```text
src/
├── lib/
│   └── components/
│       ├── layout/
│       │   ├── PublicLayout.svelte    # обёртка для публичных страниц
│       │   ├── AdminLayout.svelte     # обёртка для админки
│       │   ├── Logo.svelte            # логотип «ВСЯК»
│       │   ├── Footer.svelte          # футер (политика конфиденциальности)
│       │   ├── Sidebar.svelte         # боковое меню админки
│       │   └── TopBar.svelte          # верхняя панель админки
│       └── ui/                        # базовые контролы (Button, Input и т.д.)
├── routes/
│   ├── +layout.svelte                 # корневой layout (общие стили)
│   ├── +page.svelte                   # лендинг
│   ├── thanks/+page.svelte
│   ├── privacy/+page.svelte
│   ├── auth/
│   │   ├── login/+page.svelte
│   │   ├── register/+page.svelte
│   │   └── reset/+page.svelte
│   ├── partner/
│   │   ├── +layout.svelte             # PartnerLayout (дашборд + навигация)
│   │   ├── +page.svelte               # дашборд партнёра
│   │   ├── referral/+page.svelte      # реферальная ссылка
│   │   └── invites/+page.svelte       # приглашённые
│   ├── feedback/
│   │   └── +page.svelte               # отзыв клиента (1–5★)
│   └── admin/
│       ├── +layout.svelte             # AdminLayout (top bar + sidebar)
│       ├── +page.svelte               # дашборд
│       ├── leads/+page.svelte         # заявки (таблица, статусы, экспорт)
│       ├── services/+page.svelte      # услуги
│       └── partners/+page.svelte      # партнёры (апровал, рейтинг)
```

**PublicLayout.svelte:**

```svelte
<script>
  import Logo from './Logo.svelte';
  import Footer from './Footer.svelte';
</script>

<div class="min-h-screen bg-[#F9F7F2] flex flex-col items-center px-5 py-8">
  <Logo />
  <main class="w-full max-w-[480px] flex-1">
    <slot />
  </main>
  <Footer />
</div>
```

**AdminLayout.svelte:**

```svelte
<script>
  import TopBar from './TopBar.svelte';
  import Sidebar from './Sidebar.svelte';
</script>

<div class="min-h-screen bg-[#F9F7F2]">
  <TopBar />
  <div class="flex">
    <Sidebar />
    <main class="flex-1 p-6 ml-0 md:ml-60">
      <slot />
    </main>
  </div>
</div>
```

**Использование в маршрутах:**

```svelte
<!-- routes/+layout.svelte (корневой) -->
<slot />

<!-- routes/thanks/+page.svelte -->
<PublicLayout>
  <h1>Спасибо!</h1>
  <p>Ваша заявка принята.</p>
</PublicLayout>

<!-- routes/admin/+layout.svelte -->
<AdminLayout>
  <slot />
</AdminLayout>
```

### 2. Общий layout для админ-панели

Используется на всех страницах /admin/*

Структура:
- Верхняя панель (top bar):
  - Слева: логотип «ВСЯК» (маленький)
  - Справа: имя пользователя + кнопка выхода
- Боковое меню (sidebar) слева (на мобиле — выезжающее):
  - Дашборд
  - Заявки
  - Услуги
  - Партнёры
- Основная рабочая область справа:
  - Заголовок страницы
  - Контент (таблицы, карточки, формы)
- Фон рабочей области — кремовый #F9F7F2
- Карточки и таблицы — белые с мягкими тенями
- Акцентный цвет кнопок и активных пунктов меню — шалфейный #6B9B7A

Стиль админки: чистый, спокойный, профессиональный, без визуального шума.

### Страницы для отрисовки

Публичные:
1. Лендинг (/) — логотип + описание + карточки услуг + форма заявки
2. Страница «Спасибо» (/thanks)
3. Политика конфиденциальности (/privacy)

Админские:
4. Логин (/admin/login) — центрированная белая карточка
5. Дашборд (/admin) — статистика + таблица заявок
6. Управление услугами (/admin/services)
7. Управление партнёрами (/admin/partners)

Общие требования:
- Mobile-first
- Мягкие скругления
- Хорошая читаемость
- Аккуратные состояния (hover, focus, disabled, loading, empty, error)
- Единый визуальный язык между публичной частью и админкой

## Страницы для реализации

### 1. Лендинг (публичная часть) — `/`

Единая страница, скроллинг-based. Фон: warm cream (#F9F7F2).

**Hero-секция:**
- Логотип «ВСЯК» (центр, крупный)
- Краткое описание: «Быстро найдите мастера для вашего дома» (или similar)
- Много whitespace, чистое пространство

**Секция «Наши услуги»:**
- Заголовок: «Наши услуги»
- 3 карточки в вертикальном стеке (на мобилке)
- Каждая карточка: белый фон,soft rounded corners (12px), subtle shadow
- Иконка (soft sage green), название услуги, короткое описание
- Карточка кликабельна → подскролливает к форме и выбирает услугу

**Секция «Оставьте заявку» (CTA + форма):**
- Заголовок: «Оставьте заявку»
- Поля формы (soft rounded corners, warm gray borders):
  - Имя (текстовое поле, обязательное)
  - Телефон (input type="tel", обязательное, маска +7 (XXX) XXX-XX-XX)
  - Выбор услуги (выпадающий список, привязан к карточкам выше)
- Чекбокс: «Я согласен на обработку персональных данных» (обязательный)
- Кнопка «Отправить заявку» — large, full-width, soft sage green (#6B9B7A), white text, border-radius 24px
- Под кнопкой: мелкий текст «Нажимая «Отправить», вы соглашаетесь с политикой конфиденциальности»

**Состояния формы:**
- **Idle:** поля пустые, кнопка активна
- **Loading:** кнопка показывает спиннер, поля заблокированы
- **Success:** форма скрывается, показывается блок «Спасибо! Ваша заявка принята.» с иконкой галочки (soft mint #81B29A)
- **Error:** красная подпись под формой + кнопка «Повторить»

**Поведение:**
- При переходе по `/?ref=TOKEN` — токен сохраняется в cookie (30 дней)
- При выборе услуги из карточек — форма автоматически прокручивается

### 2. Форма отзыва — `/feedback`

- Заголовок: «Оцените качество услуги»
- Рейтинг: 5 звёзд (кликабельные, soft sage green при выборе)
- Комментарий (опционально, textarea)
- Кнопка «Отправить отзыв»
- Состояния: Loading, Success («Спасибо за отзыв!»), Error

### 3. Авторизация

**Вход — `/auth/login`:**
- Логотип «ВСЯК» (сверху)
- Поля: email, пароль
- Кнопка «Войти» (soft sage green)
- Ссылки: «Забыли пароль?», «Регистрация»

**Регистрация — `/auth/register`:**
- Поля: имя, email, пароль, подтверждение пароля
- Кнопка «Зарегистрироваться»

**Сброс пароля — `/auth/reset`:**
- Поле: email
- Кнопка «Отправить ссылку»

### 4. Админ-панель — `/admin`

**Дашборд (главная):**
- Верхняя панель: общее число заявок, заявки за сегодня, число партнёров
- Таблица заявок: дата, имя, телефон, услуга, источник, статус, рейтинг клиента
- Фильтры: по дате, услуге, партнёру, статусу
- Кнопка «Экспорт в CSV»

**Управление услугами — `/admin/services`:**
- Список услуг: название, описание, цена, статус
- Кнопки: «Добавить», «Редактировать», «Скрыть/Показать»

**Управление партнёрами — `/admin/partners`:**
- Список: имя, email, статус, рейтинг, approval_status, регион
- Кнопки: «Добавить», «Одобрить/Отклонить», «Заблокировать», «Копировать ссылку»
- Детали: `/admin/partners/:id` — полная статистика, рейтинг, история

**Управление админами (суперадмин) — `/admin/admins`:**
- Список админов: имя, email, роль, регион
- Кнопка «Добавить админа»

### 5. Личный кабинет партнёра — `/partner`

**Дашборд:**
- Число заявок, конверсия, рейтинг
- Список последних заявок

**Реферальная ссылка — `/partner/referral`:**
- Ссылка: `site.com/?ref=TOKEN`
- Промо-код
- Кнопка «Копировать»

**Приглашённые — `/partner/invites`:**
- Список приглашённых партнёров: имя, дата регистрации, статус

## UI-контролы и формы

Реализуй единый механизм UI-контролов, который используется как в административном интерфейсе, так и в пользовательских формах.

Основной принцип: **минимальная сложность и минимальный объём собственного кода**. Не создавать собственную UI-библиотеку или избыточную abstraction layer.

### Архитектура

1. **Native HTML controls** как базовый уровень:
   - `button`, `input`, `textarea`, `select`, `checkbox`, `radio`, `file`, `label`, `form`

2. Поверх native HTML — небольшие переиспользуемые Svelte-компоненты только там, где это действительно необходимо:
   - `Button`, `Input`, `Textarea`, `Select`, `Checkbox`, `RadioGroup`, `Switch`
   - `FormField`, `Form`, `Modal/Dialog`, `Dropdown`, `Tabs`, `Pagination`

3. Не создавать отдельные компоненты для каждого экрана. Компоненты должны быть **generic и переиспользуемыми**.

4. Не дублировать контролы между admin и public UI. Один базовый набор компонентов, разные варианты (`variant`, `size`, `density`).

### Стилизация

Единый механизм design tokens:
- colors, spacing, typography, border radius, shadows
- focus states, disabled states, error states

Стили задаются централизованно. Не использовать inline styles без необходимости.

### Формы

Композиция контролов:

```text
Form
 ├── FormField
 │    ├── Label
 │    ├── Input
 │    ├── Description
 │    └── Error
 ├── FormField
 │    └── Select
 └── FormActions
```

Контрол отвечает только за UI и взаимодействие. Валидация и бизнес-логика — снаружи.

### Состояния контролов

Все интерактивные контролы поддерживают:
- default, hover, focus, active, disabled, readonly
- loading (если применимо), error, success

Keyboard navigation и accessibility — обязательны. Семантический HTML + ARIA где необходимо.

### Headless / сложные контролы

Для компонентов, где native HTML недостаточен (`Dialog`, `Combobox`, `Popover`, `DatePicker` и т.п.), использовать небольшие проверенные Svelte primitives/headless-компоненты.

Предпочтение решениям:
- не навязывающим визуальный дизайн
- хорошо интегрирующимся со стилями
- с accessibility
- с минимальным runtime

Не подключать полноценную UI-библиотеку только ради нескольких простых контролов.

### Ограничение abstraction

Не создавать компоненты «на будущее». Каждый компонент — реальное использование минимум в 2 местах.

Не создавать: `UniversalControl`, `BaseComponent`, `AbstractInput`, `UIManager`, `ControlFactory` и аналогичные избыточные абстракции.

Целевая архитектура:

```text
HTML
  ↓
small reusable Svelte components
  ↓
Form / Admin components
  ↓
Pages
```

### Важно

Перед добавлением UI-библиотеки проверь, можно ли реализовать контрол семантическим HTML + небольшим Svelte-компонентом. Подключать зависимость только если она даёт существенную функциональность, которую сложно реализовать самостоятельно — особенно для интерактивных компонентов с доступностью (accessibility).

## Требования к дизайну

### Mobile-first
- Все элементы — крупные touch-targets (минимум 44×44 px)
- Форма — не более 3 полей (имя, телефон, услуга)
- Телефон — нативная клавиатура (`type="tel"`)
- CTA-кнопка — занимает всю ширину на мобилке
- Адаптация от 320px до 1440px

### Визуал
- Clean, warm, premium, много soft whitespace
- Мягкие тени, скруглённые углы, modern and friendly
- Без резких контрастов (no harsh contrasts)
- Мягкое естественное освещение (soft natural lighting)
- Cream background (#F9F7F2)

### Состояния
- Loading: спиннер на кнопке + заблокированные поля
- Success: анимация появления галочки
- Error: красные рамки на полях + текст ошибки под полем
- Пустое состояние: «Заявок пока нет»
- Партнёр без апровала: серый бейдж «На модерации»

### Доступность
- Все поля формы имеют `<label>` (визуально скрытый, доступный для screen reader)
- Кнопки имеют текст (не только иконки)
- Цвет не единственный индикатор статуса (иконка + текст)

## Не реализовывать (V2)

- Онлайн-оплата
- Интеграция с CRM
- Мобильное приложение

## Примечание

- Бэкенд (API) будет подключён позже — пока используй mock-данные
- Реферальная логика (cookie, attribution) — на фронте только сохранение токена, вся логика атрибуции на сервере
